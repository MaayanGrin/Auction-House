# Auction Client (Angular 17)

## Setup
- Ensure Node.js installed.
- npm install
- To run: npm start (uses ng serve). If '@angular/cli' is not installed globally, the devDependencies include it.

The client connects to http://localhost:4200 by default.
